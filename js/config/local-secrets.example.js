/**
 * Local developer secrets (example)
 *
 * Copy this file to:
 *   js/config/local-secrets.js
 *
 * That file is gitignored and loaded automatically at startup.
 */
window.BooksWithMusicSecrets = {
  // Cyanite integration token used for Spotify discovery
  cyaniteAccessToken: '',
};
